# {{ ansible_managed }}
events {
    worker_connections 1024;
}

http {
    server {
        listen      {{ nginx_port_to_listen }} default_server;
        server_name default_server;
        root        /usr/share/nginx/html;

        location / {
        }
    }
}